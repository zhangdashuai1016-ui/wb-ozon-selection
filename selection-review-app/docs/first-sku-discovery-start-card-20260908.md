# 当前首件路线（2026-09-09，替代下方历史启动卡）

主人已明确选择 **Seerfar 市场研究＋原1688供货核验**，无需再次选择供应方。LinkFox未获主人采用批准；下方30＋13积分及其查询计划退出当前首件方案，仅保留工程历史，不能据其配置、创建批次或发起查询。现有部署的相关配置仍为空。

当前准确事实与下一步见 [Seerfar原路线核对](first-sku-seerfar-return-20260909.md)，本轮决定见 [版本化决定](first-sku-current-decisions-20260909.json)。文档证书问题不等于真实API或账号不可用；实际账号、额度和商品接口未测。已有版本已经部署，但本轮新修复尚未部署，下方“尚未部署”仅指历史当时。

原批准路线受阻时应先准确说明失败层并解决原路线；更换供应商、模型、数据源或业务方案，必须先呈现原因、方案和影响并取得主人决定，再实施。备选、未启用或未收费都不是绕过该决定的理由。

---

# 首件主动发现启动卡（工程已接线，真实服务未启用）

## 当前可审阅范围

2026-09-08 续轮：已把 LinkFox 固定公开合同接入正式软件作业。此前 Seerfar 直连证书故障、缺关键词连接器和“源码不改”的段落均是历史探索记录，不再代表当前工程状态。真实商品、账户、凭据、付费、写店和部署仍未发生，首件 A→E 未完成。

本轮软件提出的方向仍为普通非电桌面整理小件。准确可执行的计划草案见 `first-sku-discovery-plan-20260908.json`；它不是已批准计划，未安装到运行环境。

| 步骤 | 本次限定动作 | 预算与决定 | 保存结果 |
|---|---|---|---|
| 搜索 | Ozon关键词第一页最多20条；1688两个关键词各第一页最多10条，共3次，不翻页 | 独立搜索许可，最多30 LinkFox积分 | 按服务返回顺序查重，最多1个待核验候选；已淘汰商品不复活 |
| 详情 | 保存候选的Ozon商品1次、第一组供应搜索中首个offer详情1次 | 独立详情许可，展示两个精确商品后最多13 LinkFox积分 | 两个作业均绑定同一候选版本；回执齐备后一次性更新A卡 |
| A确认 | 当前卡选择具体SKU并确认同款、商品价、国内运费、其他费用、实际成本和包装 | 主人一次完整供货确认 | 原A/B流程继续；供货方案确认不表示采购或店铺写入许可 |

搜索30和详情13是公开合同标称费用上限，不是人民币、已知余额或已发生费用。空结果、失败和超时的实际收费仍未知。已批准动作失败即停，不自动重试、换供应来源或扩大数量。详情未知结果必须先核对，现工程没有刷新或重试入口。

详情仅是第三方API证据，明确保存 `provider_api_read_only`。不伪装为买家端独立回读或浏览器采集；未返回的材质、带电、规格、起订量和包装不猜。已知品牌风险不能在普通无品牌路径确认通过。详情保存后不会再次打开1688；A卡仍使用既有正式确认和冻结链。

启用前还需：批准部署当前新版本；配置真实 LinkFox 服务及其私有凭据路由；主人在页面按具体批次和候选提交独立读取/付费许可。以上均不由本文件替代。最终源码、验证日志和准备包见 `logs/first-sku-discovery-final-quality.json`。

## 历史探索记录（保留，不作为当前阻塞）

日期：2026-09-08。目的：软件主动发现、筛选和推荐，主人不必提供商品。此卡是工程审查草案，不是已批准采集、付费或生产许可；尚未发起任何搜索。

最新范围修订：首件优先使用少量方向关键词，不要求完整无种子类目目录。下文类目路线只是既有代码可见路线；只有正式搜索合同强制要求时才补一个方向的精确 categoryId。两次公开技术检索无结果不是穷尽结论，也不表示必须等待主人提供文档链接。

本轮只读核验发现：`buildSeerfarCategoryPayload` 已有 `keywords: []`，值得先核实，但函数不接收关键词且强制 categoryId。技能还明确警告关键词过滤可能返回未过滤 Top20，不能据字段存在认定方向搜索已支持。`product_detail/search` 要求已知 SKU；`backSearch` 由 SKU 反查词，均不等于关键词找商品。

现 extension Worker 只接受 URL 与 expectedProductId/expectedOfferId，没有关键词搜索 mode 或结果 DTO。技能中的买家端搜图是研究指引，未形成正常软件消费者。已暴露工具元数据中未发现 Seerfar/1688/Ozon 商品关键词专用 connector；通用浏览器不等于正式软件搜索连接器。

最小补证动作：官方入口已定位为 https://doc.seerfar.cn/api-docs.html?lang=zh-CN ，但普通HTTPS和Chrome独立新标签均被证书域名不匹配阻止，未取得正文。需官方提供正常可访问的公开文档或版本化下载，明确关键词过滤、类目是否必填、商品响应及费用；已准备 `logs/first-sku-discovery-20260908/official-doc-access-request-draft.md`，未发送。主人无需理解 API 或提供商品。公开技术文档尝试不涉及商品、账号、凭据或付费调用。

## 已确定的意图与拟议有限范围

下表数量是本次拟议预算，尚未获准为正式计划，不写成默认业务规则。

方向由软件提出：普通非电桌面整理小件，偏好单件供货、结构简单、易包装和低品牌风险。这是筛选偏好，不能据此认定实际商品非电、无侵权或尺寸合格。未知事实保存为 unknown。

| 网站/服务 | 有限动作及数量 | 需要的许可 | 停止点 |
|---|---|---|---|
| Seerfar Open API | 一个精确类目第一页最多 20 个市场候选；额度前读、类目查询、额度后读各一次，合计最多 3 次 HTTP，不翻页、不重试 | 独立 A 计划下的账户 API 读取及付费许可，明确类目、模式、次数、点数上限与期限；不沿用 C1 关键词许可 | 返回严格商品 DTO 后，本地筛选最多 3 个待核实商品 |
| www.ozon.ru | 最多 3 个精确商品详情，每个一次 | 本批次公开详情读取许可；若页面依赖登录，另有登录态读取许可才可用，不自动借用登录态 | 保存真实销售快照；验证/登录/限流立即停，不当作零需求 |
| 1688.com | 最多 2 个由已核实方向生成的关键词，每个第一页最多 10 条供应搜索结果，不翻页 | 独立搜索读取许可；实际需要登录的路径必须有明确账号登录态许可 | 合并查重后挑最多 3 个精确供应详情；当前搜索 connector 尚不存在，禁止由详情 Worker 代替 |
| detail.1688.com | 最多 3 个精确商品详情，每个一次 | 同批次、绑定 offer 身份的详情读取许可；若依赖登录须明确登录态范围 | 保存 SKU/价格/运费/一件起订/包装等实证；默认不选中、不确认 |
| 本地领域服务 | 对全部有效且可比证据筛选排序，形成 1 个推荐具体供应 SKU，保留候选方案与依据 | 版本化筛选规则；不产生外部调用 | 主人只确认最终推荐的具体供货方案；证据未齐时明确缺口，不虚构推荐 SKU |

排除：已淘汰或已存在的同身份商品；明确第三方品牌/IP风险；已知带电、液体、易碎或需要本轮之外合规证明的方向。筛选不能仅凭标题图片认定材质、功能或侵权状态。无合格项就保存本轮结论并停止，不扩数量、不复活淘汰、不自动换方向。

主人供货确认之前不得进入正式 B。B 只读冻结证据，完整费用、包装、汇率与适用门槛缺失就停；不据条件测算自动通过，不重新搜索。生产、采购、广告、客服、上传和部署均不在本卡范围。

## 必须补足的精确项

1. 当前 Seerfar 官方 category_detail 商品响应 schema：商品身份、规范 URL、价格币种、销量时间窗、卖家与类目字段。现关键词 adapter 只输出 reverse_keywords 词条，category 非空结果不能直接复用其空 candidates 输出。
2. 该方向真实 Seerfar categoryId 和无商品种子时取得它的正式方法。现代码要求 Ozon 组合类目 ID；不能拿 Seller API description_category_id 代替或自行拼接。
3. 当前收费规则及明确批准点数上限。本地参考文档记载 2026-07-27 category 10 点、quota 0 点，但未附可追溯官方文档 URL，不足以确认当前价格，不能写入本轮付款上限。
4. 正式 A 发现计划 ID、版本、批次身份、期限、目标平台/分支范围；对应各网站/登录态/付费许可。当前软件无 A discovery job/plan/permission 合同，当前可执行的有效 A 发现计划和许可均为 0；其他阶段许可不得扩用。
5. 可部署的市场商品归一化器、1688 搜索 connector 与一次性 Worker 能力声明；登录态不从开发工位取得。

## 最小工程方案与本轮冻结边界

复用现有 repository、SoftwareJob、租约、发送门、失败分类、SalesSnapshot/SupplierOption 和供货推荐规则。正式实现需要先有可验证的市场商品合同和搜索能力，再将 A 批次作为既有 SoftwareJob 的新受控 subject/类型接入；旧 C1/D/E 作业和许可仅按原合同读取，不迁移、不借用。不能用独立待办表代替队列。

本轮不新增准备存储槽、无实际调用方 service 或仅返回“未配置”的 DTO。它们不能解除当前外部合同阻塞，反而增加后续迁移。领域/本地用例方案冻结，待准确合同到齐后连同正式调用方、预算与许可一起实施。本轮仍可完成可审查的启动卡、现有入口核验和下述精确补证清单，源码不改。

随后必要测试包括：超预算在发送前停止；相同商品身份查重与淘汰记录不复活；缺证据、真实空、权限失败与系统错误分开；无具体供货必需事实不生成可确认推荐；未知卖家身份但可比、现价清楚且证据完整的销售样本仍可参加价格带，不以所有 unknown 一刀切排除。

## 最小补证清单与取得方式

向 Seerfar 官方 API 文档入口取得一份版本明确的技术资料即可，不需要任何商品或真实账户样本：

- `POST /open-api/category/detail/search/ozon`：请求模式和枚举、分页上限，以及成功/真实空/失败响应的完整商品 schema；必须明确稳定商品 ID、规范链接、价格币种、统计时间窗和缺失字段含义。
- 类目目录或枚举的正式文档：如何在没有种子商品 SKU 时取得 Ozon 组合类目 ID，类目路径和末级约束；不能由工程猜 categoryId。
- 同一接口的当前计费文档：每次或每条费用、空结果/失败/超时是否收费，以及 quota 的单位与读取成本。由此才能提出真实付费上限，历史 10 点不作为现行证明。
- 1688 搜索独立正式插件/connector 的搜索入口、登录要求与结果合同；现详情 Worker 只按已知 offer 采集，不能扩大为关键词搜索。

这些资料可以是官方公开文档准确链接或官方版本化导出。只有文档需要登录时再明确缺少资料访问权限；不以读取商品、读取密钥、调用付费接口或要求主人找商品代替。

## 本次核验记录

初次仅本地代码和参考阅读，没有官方页面请求。续轮另获最多 2 次官方域名技术检索，已于 2026-09-08 11:26 UTC 前用完：`site:seerfar.cn "API" "类目" "积分"` 与 `site:seerfar.cn "category" "open-api"`，均返回无结果。未猜文档 URL，未访问实际 API、账户或商品，也没有继续泛搜。搜索无结果不证明官方文档不存在，只证明本次未取得。

结论：现可执行的有效 A 发现计划/许可仍为 0；无法当轮正确完成主动发现。准确阻塞是官方市场合同/类目/费用和独立供应搜索能力，其次才是正式批次作业接线。方向已由软件提出，主人无需提供商品。

详见 `logs/first-sku-discovery-20260908/contract-review.json`。源码、封存包和业务数据未修改。

## 官方文档入口已定位

根方从官网 https://www.seerfar.cn/category/tutorials/ 的 Open API 导航取得 https://doc.seerfar.cn/api-docs.html?lang=zh-CN ，无需主人提供 URL。普通HTTPS因文档域名证书不匹配停止，内置浏览器工具不可用；最后一次Chrome独立新标签尝试也返回 `net::ERR_CERT_COMMON_NAME_INVALID`，未取得正文后停止。未绕过证书校验、未登录、未调用商品或付费 API。记录见 `logs/first-sku-discovery-20260908/official-doc-fetch.json`。当前限制是文档可达性；不能据此断言关键词接口缺失或要求完整类目目录。

## 新增可实施路线：显式选择 LinkFox provider（尚未启用）

固定公开仓库版本 `93a1dbf71f793169db2b01928ec385fe1f7bd843` 的合同已落盘。它是独立服务，不冒充 Seerfar 直连，不作为失败后的自动替代。Ozon商品筛选支持标题关键词，只有page必填，不需要完整类目目录；1688支持中文关键词和每页10条。一次市场查询、两次供应搜索的文档价格合计30 LinkFox积分（12+9+9），不等于人民币、当前账号余额或Seerfar点数，空/失败计费仍未知。

先实现严格API合同及实际调用它的单次connector，随后继续接现SoftwareJob和明确计划/预算/许可，不把单adapter当A完成。配置留空、真实调用为0。供应搜索只产生offer候选，不证明SKU/运费/包装/精确同款，仍需详情证据及主人供货确认。

发布仓库自述与LinkFox域名一致，官网首页确为LinkFox Skills；未找到官网反向GitHub链接，不声称GitHub组织已核验。文档证据见 logs/first-sku-discovery-20260908/linkfox-contract-findings.json。客服草稿不是唯一道路，前文Seerfar直连证书失败不阻止本路线工程。

### 首件详情接入（独立于搜索许可，尚未真实调用）

固定 LinkFox 合同版本已有两条单品详情接口：Ozon `productDetailSearch` 与 1688 `productDetail`。首件先配置最多 1 次 Ozon 详情和 1 次精确供货详情，合同标称合计 13 LinkFox 积分；这不是已授权预算，也不是已发生扣费，失败或空结果的实际扣费仍为 unknown。不会默认执行此前讨论的 6 次详情方案。

- Ozon 详情可以形成明确标注 `provider_api_read_only` 的 SalesSnapshot，保留商品 ID、价格、币种、图片和类目来源。不能声称这是买家页面独立回读；完整商品属性、卖家跨境身份不猜。出现品牌信息进入品牌核对，不能直接无品牌放行。
- 1688 详情保留逐 SKU `skuId`、`specId`、单件 `retailPrice` 与字段来源；批发 `price` 不补单件零售价。未证明的运费、实付总价、实际包装、材质和带电属性保持 unknown，供货默认未确认。
- 软件按批准计划的 `provider_order` 顺序，在有限已返回候选内查重并排除淘汰历史，最多新建 1 张待核验 A 卡。搜索结果不会直接成为 A 通过或供应 SKU 确认。

本地工程已有严格请求/解析、既有 A 对象适配与测试；真实传输、独立详情许可与持久作业仍需完成接线并明确启用。当前真实商品查询、凭据读取、付费调用均为 0。证据见 `logs/first-sku-discovery-20260908/linkfox-detail-acquisition.json` 与 `linkfox-detail-findings.json`。
