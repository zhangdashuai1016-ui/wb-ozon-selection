# 首件工作台启用与回滚审查卡

2026-09-08。状态：工程准备，尚未批准或执行部署。此卡不授予商品采集、凭据读取、付费、上传、写店或采购权限。首件真实 A→E 尚未完成。

## 本次可审阅的动作

先启用新版本地工作台及已准备的网关修复，保留现有业务记录；所有外部消费者与店铺写入绑定为空。目标是让主人看到准确的软件能力、历史记录和当前缺口。该动作不会开始选品或自动恢复历史作业，也不代表首件可以立即上架。

| 项目 | 准确范围 |
|---|---|
| 工作台源 | 当前9ce7施工目录；本轮搜索、详情、B精确复算与D接E的最终清单和新包收据见 `logs/first-sku-discovery-final-quality.json` |
| 新版代码位置 | `/Users/shuaizhang/Library/Application Support/今日选品评审台-versions/20260908-product-discovery-v6`，须在批准执行时确认不存在后新建 |
| 旧版保留 | `/Users/shuaizhang/Library/Application Support/今日选品评审台` 的源码保留；不覆盖旧封存包 |
| 工作台服务 | `com.shuaizhang.selection-review-app`，端口4317；只更新其启动文件的代码路径及本卡明确环境 |
| 候选/生命周期/作业 | 继续使用 `/Users/shuaizhang/Library/Application Support/今日选品评审台/data/candidates.json`，不复制为空库、不自动迁移或重跑旧候选 |
| 流程图及最终素材 | 继续使用旧根 `data/workflow-map.json` 与 `data/c2-final-uploads` 的准确路径 |
| 历史图片 | `product-images` 当前固定由代码根读取。冷停后按原结构完整复制到新版代码根，比较文件清单和字节；仅修改数据目录环境变量不足以保留这些图片 |
| 主人身份 | 复用已实现的 `local_owner_password`；拟私有文件 `/Users/shuaizhang/Library/Application Support/selection-review-owner-identity/owner.json`，与代码/业务数据/素材隔离；不读取秘密，主人在页面完成首次设置 |
| 网关修复 | `logs/gateway-repair-20260908/gateway-repair.patch` 的11文件准确改动，原源码 `/Users/shuaizhang/Documents/电商能力实验室/ecommerce-ai-gateway`；先冷备份再应用，未列文件保持原状 |
| 网关服务 | `com.shuaizhang.ecommerce-ai-gateway`，端口4318；保留现有启动参数及 `ECOMMERCE_AI_GATEWAY_DATA_DIR=/Users/shuaizhang/Library/Application Support/ecommerce-ai-gateway` |
| 明确不改 | 4173证据服务、其他项目/店铺、浏览器登录态、钥匙串条目、账单、供应和平台商品 |

网关启动会核对中断作业并可能写持久状态，不能当作纯只读操作。网关 `/health` 和 `/v1/capabilities` 当前会读取钥匙串，**本卡验证不调用它们**。本轮仅检查监听/进程/静态版本与已有状态文件；要检查密钥可用性，须另有准确凭据读取范围。

## 冷停、备份与执行顺序

1. 执行前重新核对两个服务的代码路径、启动标签和当前作业状态。若有排队、运行、平台处理中或外部结果未知，先停止该部署步骤并按原作业对账；不以旧快照证明现在空闲。
2. 使用准确启动服务标签停止两个服务，等待退出并确认端口释放。两者都有 KeepAlive，不能只杀进程后任其自动重启。
3. 在独立备份目录完整冷备份工作台状态、流程图、最终素材、历史图片、两份启动配置、网关当前源码及 `inference-jobs.json`。同时记录文件清单与字节比较结果；不将秘密内容打印、提交或加入运行包。备份尚未创建。
4. 安装新的工作台代码包，保留已有持久路径，复制历史图片。网关13项原始来源重新比较一致后，才应用已审11文件修复；若源发生变化立即停止，不机械覆盖。2026-09-08 11:26:45 UTC 核对13/13一致，补丁只检查通过，证据见 `logs/gateway-repair-20260908/activation-source-verification.json`；执行时仍须重验。
5. 工作台启动环境显式使用本地模式、JSON适配器、4317回环地址、旧持久路径、独立身份文件。`SELECTION_REVIEW_AUTO_DELIVER=off`、`SELECTION_REVIEW_CODEX_DISPATCH=off`、`CODEX_OFFLINE=true`；初始化新数据文件关闭。搜索、详情、关键词、AI草稿、D/E、账户读取/发现服务以及生产/凭据路由均留空，D观察和E回读轮询不配置，OSS不配置。新A搜索/详情启动对账只归类已有中断作业，可能保存failed/unknown终态；不重发外部请求。
6. 先启动网关、再启动工作台。核对监听/运行代码路径；工作台只读 `/api/health` 与页面登录状态；主人完成登录后只读核对候选数量、ID/revision、淘汰记录、已保存作业、素材引用及能力缺口。不要打开会读凭据的网关健康页。
7. 保存独立启用回执，逐项区分包准备、代码安装、进程启动、页面可用、持久状态一致和平台业务完成。无对应证据的项保持未验证。

上述路径、配置和启动动作必须作为一个明确部署决定批准；本文件本身不会执行。真实采集/付费/生产必须由当前批次或SKU正式许可和相应软件作业另行触发。

## 回滚

- 若能证明冷备份后没有任何业务写入及外部请求：停止新服务，保存本次诊断，恢复两套旧源码/配置及同一时点状态快照，启动旧服务并只读核对。不得自动恢复已知失败或历史处理队列。
- 若已有新业务决定、作业、回执或外部动作：停止服务并保留最新完整状态，不把旧备份覆盖到新状态，不让旧代码直接打开未经验证的新schema。先核对兼容性与外部请求终态，再决定前向修复或可验证的数据回退；平台动作不能靠恢复JSON撤销。
- 回滚有任何失败就准确停止并保留两侧数据，不自动换路径、重发或删除证据。

## 首件真正还缺什么

1. A搜索和详情的软件作业、独立许可、真实服务接线及同一A卡应用已完成本地验证；尚无真实LinkFox凭据路线、批准批次或真实调用。公开合同来源及准确30+13积分分步范围见主动发现启动卡，Seerfar直连证书错误不再是本路线工程阻塞。
2. B补齐精确费用后的独立复算已接通，不重发A确认；正式通过才原子创建C1。C1关键词消费者、已成功证据接本地草稿准备及配置修复后的“继续准备文案”入口已通过隔离服务/HTTP验证。真实费用/来源、运行配置、单次付费及C2最终素材尚未验收。
3. D/E 仍缺库存 `price_sent` 的精确机器来源及普通SKU库存 `quant_size` 字段语义、官方在售机器状态、最终素材与平台转换媒体的可靠对应。现有限定官方查询未补齐，未知保持未知。
4. Miska 稳定店铺身份、当前后台写价币种、真实仓库/凭据路线和首个SKU/revision尚未核验；仅在实际步骤需要时处理，不新增身份体系。
5. 本机JSON和单Worker仍是本地开发能力，未验收多人中央运行。员工电脑关闭后的中央状态可用性不能从本地测试推定。

## 本轮实况

只读当前旧数据时共有52个历史候选、38条历史派发；未发现运行/排队的处理记录。网关保存2个已完成作业。以上只是当时快照，部署前必须重新读取；不将其中任何旧Miska候选作为首件自动重启。

真实商品/账户/凭据访问、付费、上传、平台写入及部署/重启均为0。公开技术文档曾正常读取；Seerfar直连证书错误留为历史，LinkFox固定合同路线已接线，支持草稿未发送。旧v3/v4/v5封存包未更改；新包及最终源码见本轮 `logs/first-sku-discovery-final-quality.json`。本卡为可审阅部署方案，不是启用回执或首件完成证明。
