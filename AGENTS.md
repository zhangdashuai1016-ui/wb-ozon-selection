# WB 与 Ozon 选品项目规则

## 0. Codex Independence（最高架构不变量）

- 任何正常 SKU，只要输入完整、外部依赖可用，并且已经取得当前步骤所需的人类授权，就必须能够在 Codex 完全关闭、没有任何 Codex 会话、代理或任务运行的情况下，完成 A → B → C1 → C2 → D → E 中所有应由软件完成的步骤。
- Codex 不得成为正常生产链路中的调度器、状态机、监控器、队列消费者、数据搬运者、API 调用者、浏览器操作者、阶段推进者或正常业务结果提供者。正常业务需要 AI 时只调用正式 AI 网关；需要读取平台时只调用正式连接器、API、插件或软件后台作业。
- 已知业务失败和已知技术失败必须由软件分类、持久化并安全停止。不得自动重试、自动换模型、自动换路径或唤醒 Codex。Codex 离线不得导致状态丢失、重复执行、重复扣费、半套写入或无法判断卡点。
- 只有现有软件规则无法处理的真正未知系统异常，才允许持久化 ExceptionCase。创建 ExceptionCase 只表示需要技术维护，不表示 Codex 已经在线或已经领取；只有真实维护轮次编号落盘后，才能显示 Codex 正在介入。
- ExceptionCase 必须绑定 candidate、SKU、revision、生命周期阶段、软件作业、最后一个确定成功步骤、失败类型、安全证据引用、外部请求终态、是否 unknown_outcome 以及禁止自动重试的动作。不得保存 Token、Cookie、密码、完整 Key、原始供应商响应或其他秘密。
- 主人商业确认、外部依赖故障、已知技术失败、商品业务结论和真正需要技术维护必须分别显示，不得统一写成“等待 Codex”或“已停止”。
- Codex 可以作为开发与维修工位，负责写代码、审查、测试、部署修复和事后诊断，但不得通过维护动作代替正常业务推进或生成正常流程所需结果。
- 只有用户明确发起一次研究或实验时，Codex 才可以协助寻找商品。研究结果只能作为待核验材料进入正式 A 阶段，不得直接形成 A 通过、B 利润结论或生产授权。发起研究也不自动授予外部采集、登录态读取、付费调用或平台写入权限。
- 每个阶段的正式验收都必须回答：“Codex 完全退出以后，这个正常 SKU 步骤还能不能完成？”答案为否时，该阶段只能标记为尚未完成软件化，不得以模块存在、接口存在或测试数量代替结论。
- 项目必须保留可执行的 Codex 离线门禁。离线测试中，正常生产路径一旦尝试派发、等待、调用或依赖 Codex，必须以 “Normal production path attempted Codex dependency.” 明确失败；不得使用假的 Codex mock 继续完成业务来制造通过。

## 0A. Multi-user Central Runtime（最高架构不变量）

- 当前项目可以继续在本机开发和进行单 SKU 验证，但正常业务架构不得永久依赖某一台个人电脑、某个本地用户、某个浏览器窗口或固定回环地址。未来多人内部使用时，核心业务状态、SKU 生命周期、作业编排、身份、权限、审计和异常记录必须以中央服务为唯一内部事实来源。
- 正确关系固定为：中央服务器是大脑，本地电脑和浏览器 Worker 是受控手脚。Web 前端只负责显示、输入和提交人类决定；本地 Worker 只执行中央服务明确分配且与自身能力匹配的作业；二者均不得保存唯一业务真相、自行决定阶段或绕过中央服务推进 A → E。
- 中央服务负责稳定实体身份、revision、作业队列、幂等、并发、租约、AI 与连接器编排、ExceptionCase、操作记录和中央数据库。正式 AI 网关保持独立边界；员工浏览器不得持有模型密钥。
- 本机 Worker 必须具有稳定 workerId，声明可验证能力，只领取分配给自己的作业，并回传结构化结果和脱敏证据。Cookie、Token、密码、VPN 凭据和浏览器登录态原则上留在本机；中央服务只保存能力、登录有效性和脱敏失败分类。
- 所有业务写操作必须绑定当前 revision。重复点击和网络重发不得重复创建作业、调用 AI 或 Seerfar、产生扣费、写平台或推进阶段。
- Worker 领取必须有租约。掉线时必须区分请求明确未发出、明确失败、平台仍在处理、结果未知以及已成功但未回传，禁止简单自动重发。
- 当前 JSON、单进程队列、localhost、macOS 钥匙串和单 Worker 可以作为明确标注的本地开发适配器继续使用，但领域与用例代码必须依赖可替换的存储、配置、身份、作业和 Worker 边界。
- JSON 原子改名只能防止意外半文件，不得作为多人并发控制；文件哈希只用于检测意外损坏或部署前后基线变化，不得冒充恶意防篡改。
- 正常生产状态若只存在于 React 内存、Local Storage、插件临时存储、Worker 临时文件或 Codex 对话，必须以 “Production state has no central persistence boundary.” 失败。
- 新增正常业务代码若把固定回环地址、个人目录、固定用户或固定 Worker 写成不可配置业务事实，必须以 “Local machine dependency entered production domain.” 失败。
- 多人化必须小步实施。未经迁移验收，不得把当前单机 JSON、进程内队列或单 Worker 描述成多人中央运行能力。
- 所有阶段除 Codex Offline 验收外，还必须回答：“一台员工电脑关闭后，中央状态是否仍准确，其他用户是否仍可继续？”答案为否时，只能标记为本地开发能力。

## 1. 权威事实、人类入口与项目范围

- 今日选品评审台是人类查看、判断、补充资料、确认方向和发现问题的主要业务入口，但 UI 不是最高事实来源。
- 事实优先级固定为：
  1. 平台独立回读或官方当前证据；
  2. 中央持久化状态和正式回执；
  3. 已批准的版本化规则配置；
  4. UI 派生视图；
  5. 文档和聊天线索。
- 平台商品、价格、库存、图片、审核和销售状态以官方当前证据或 E 阶段独立回读为准；中央持久化状态是项目生命周期、授权、作业和 revision 的内部事实来源；UI 只能展示和提交，不能反向覆盖更高等级事实。
- 聊天可以记录用户作出的项目、产品、规则和代码决定，并作为更新正式载体的依据，但聊天本身不得成为候选状态、供应 SKU 确认、生产授权、平台写入授权、外部采集授权、失败重试授权或部署授权。
- 本项目内的对话、任务、规则和自动化都必须服务于评审台和中央持久化状态，不得形成一套只存在于聊天里、UI 看不见的平行流程。
- AGENTS.md 只保存长期有效的业务不变量、状态含义、授权边界、证据原则和展示原则。候选状态、当前数值、临时判断、实现进度和频繁变化的门槛、权重、按钮、页面分组、触发频率及实验策略不得写成永久规则。
- 具体页面分组、按钮名称、按钮位置和交互布局由产品设计和版本化配置决定，不在 AGENTS.md 中固定。
- 评审台本身不等于 Ozon 或 WB 店铺写入。页面显示、服务健康、队列数量或本地状态不能证明平台业务完成。
- 本项目只交付已经确认并可追溯的供货方案，不负责真正向供应商付款或下单。真实采购必须进入独立采购流程，重新取得采购授权，并独立回读采购结果。A 阶段供货确认不得冒充采购订单授权。
- 第三方多模态或 API 只承担软件明确调用的单步能力。输入、输出、服务版本、证据来源、适用范围和失败层必须写入数据包。模型输出默认是待核验结果，不能替代商品事实、主人确认、平台当前证据或生产授权。

## 2. 总控、领域工位与唯一负责人

- 人类主要与总控沟通。总控负责理解需求、协调领域边界、汇总真实进展并提出必须由人类决定的最小问题；总控不得与执行任务重复处理同一商品。
- 软件施工期的领域分工为：
  - 总控负责总体架构、跨阶段数据契约、评审台、共享状态、集成和最终验收；
  - 选品任务负责 A/B 领域规则、实现和领域验收；
  - 上架任务负责 C1/C2/D/E、生产适配器和平台回读的领域规则、实现与验收。
- 跨领域共享模块由总控协调。禁止多个任务同时修改同一模块，禁止以总控施工代替领域任务验收。
- 选品与利润任务负责 A 市场和供货方案、主人供货确认，以及 B 的正式利润结论；不得执行店铺写入，也不得执行真实采购下单。
- B 正式利润通过后，由软件在同一原子提交中创建且只创建一次 C1 交接，不再要求主人点击“开始上架准备”。
- 上架任务保留 C1/C2/D/E 的领域开发、规则验收和真正异常维护职责，但不得成为正常 SKU 的队列消费者或阶段推进者。
- 同一商品、同一 SKU、同一软件作业、同一源文件、同一 Chrome 用户资料和同一平台写入，在同一时刻只能有一个明确负责人。
- 负责人、当前阶段、业务结果、技术状态、主人下一步、卡点和最近实质进展必须在评审台可见。
- 新对话或新任务必须先读取当前中央或评审台数据和 revision，恢复已有候选；不得只凭旧聊天重新建卡、重新研究或重复询问。
- 已有资料只有在以下条件全部满足时才能复用：
  - 属于同一款商品和同一明确规格；
  - 当前仍然有效；
  - 来源清楚且可以追溯；
  - 数据身份与目标平台、店铺、SKU 和 revision 一致。
- 过期、规格不同、身份不一致、来源不清或证据不完整的资料不得复用。
- 执行任务在完成、出现有证据的阻塞或需要业务决定时向总控汇报，不得因普通状态更新反复打扰人类。
- 选品和上架任务是施工期的领域工位，也是软件运行后的监督与异常处理工位；成熟流程中的正常 SKU 应由软件、正式 AI 网关和受控连接器执行。

## 3. 生命周期、版本与状态隔离

### 3.1 阶段

- A：方向、市场和供货方案。判断市场、价格带、需求、竞争、理论利润空间，找到精确供货方案并取得主人确认。
- B：具体平台和店铺下的正式 SKU 利润。使用冻结市场证据、完整应计成本和实际包装数据计算正式利润。
- C1：上架事实、类目、Schema、关键词、合规和 SEO 草稿。
- C2：素材生命周期、最终商品确认卡和生产授权准备。
- D：生产执行。只按不可变生产授权执行建卡、媒体、价格、库存、发布或审核提交。
- E：独立回读。通过官方接口、卖家后台或买家端独立确认平台商品及其真实状态。

### 3.2 规范对象链

- 规范对象链为：

  OpportunityPackage → SalesSnapshot / SupplierOption → OwnerSupplyConfirmation → SkuLifecyclePackage → ProfitModel → C1ProductPlan → C2素材与最终商品确认卡 → ProductionAuthorization → ProductionPlan / execution intent → ProductionRecord或ExternalListingRecord → EVerificationRecord

- 一个主人确认的供应 SKU 可以作为共享、只读、可追溯的货源证据，被多个目标分支引用。
- 从 SkuLifecyclePackage 和 ProfitModel 开始，每个“平台＋店铺”必须建立独立生命周期分支，分别保存利润、C1/C2 计划、生产授权、SoftwareJob、写入回执和 E 回读。
- 不同平台或店铺之间不得共享凭证、缓存、幂等键、执行作业、生产回执或 E 回读结果。跨店汇总指标只能是只读聚合。
- 多个供应 SKU 不得自动合并、自动替换或共用一个供应确认。系统可以排序和推荐，只有主人能够确认具体供应 SKU。

### 3.3 四条状态线

- 每个对象和页面必须分别表达：
  - businessPhase：A、B、C1、C2、D、E；
  - businessResult：候选、待补证据、供货方案已确认、利润通过、利润不通过、淘汰、已验证等；
  - technicalStatus：待执行、运行中、明确失败、等待平台、unknown_outcome、完成等；
  - ownerAction：无需操作、待确认、待补资料、待授权、待对账等。
- 阶段、业务结论、技术状态和主人动作不得混成一套扁平状态。
- 技术失败只能改变技术状态，不得静默改变商品方向、利润结论或淘汰结论。
- “供货方案已确认”不表示已经采购下单；“可上架”不表示已经取得生产授权；“请求已接受”不表示平台写入成功；“页面存在商品”不表示该商品由本轮创建。

### 3.4 revision 与持久化

- 所有推动状态的记录必须区分读取输入时的 sourceRevision 和原子落盘后的 resultRevision。
- 销售证据、供应证据、利润模型、关键词证据、AI 回执、授权、写入回执和 E 回读记录必须追加版本，不得覆盖历史。
- 已冻结的确认或授权只能由新 ID 和新 revision 替代，不得原地改写。
- 同一次业务提交涉及的领域状态、结果引用、幂等记录和必要审计信息必须作为一个原子单元保存。
- 外部写请求发送前必须先持久化执行意图；回传结果只能以同一 job、candidate、SKU、revision、平台、店铺和授权身份收口。
- 连接器必须先把外部响应归一化为严格 DTO 和脱敏证据，再交给领域层。领域层不得直接依赖 Cookie、Header、原始 HTML、供应商私有响应形状或进程内临时对象。
- 单 SKU 动作只读取目标 candidate、关联 SKU、目标分支、关联 job 和必要共享策略。不得为单 SKU 动作反复全量扫描、复制、序列化或哈希整个候选池。
- 队列、并发、缓存、批处理和轮询必须有上限、取消、超时和背压。

### 3.5 淘汰和旧规则候选

- 淘汰商品必须保留候选身份、主人决定、淘汰理由和必要证据，用于查重、审计和避免重复研究。
- 淘汰记录必须退出正常处理集合，不再进入市场采集、供应搜索、利润复算、AI/Seerfar 作业、队列领取、自动重试或阶段推进。
- 只有主人基于当前状态明确要求重新评审时，才创建新的受控评审轮次；不得静默复活或原地覆盖历史淘汰结论。
- 旧规则产生的候选必须保留原记录并标记“旧规则、需要重新评审”，退出当前自动处理。
- 只有用户明确重启时，才创建新 revision 并引用旧记录；禁止自动补算、自动迁移、自动排队、自动推进或覆盖历史。

## 4. A 阶段：三种入口、市场和供货方案

### 4.1 三种入口

- A 阶段允许三种入口：
  - 软件按已批准计划寻找商品；
  - 用户提供供应商商品或供应链接；
  - 用户提供目标平台商品或商品链接。
- 不同入口可以跳过已经有可靠资料覆盖的重复采集，但不能跳过统一的 A 阶段数据合同。
- 三种入口进入 B 前都必须补齐统一的市场、货源、利润空间和主人供货确认材料。
- 可以复用的已有资料仅限同一款商品、同一明确规格、当前有效、来源清楚且可追溯的资料。过期、规格不同、身份不明或证据不完整的资料不得复用。
- 用户给出供应商链接时，可以不重复寻找同一货源，但仍须完成市场、可比性、成本、包装和主人确认。
- 用户给出目标平台商品时，可以不重复寻找同一目标商品，但仍须完成供货方案、SKU 匹配、成本、包装和主人确认。
- 软件自动寻找或 Codex 一次性研究得到的商品，仍须进入同一正式 A 阶段，不得形成特殊快速通道。

### 4.2 市场证据

- SalesSnapshot 至少保存平台、市场范围、卖家类型、卖家身份证据、商品 URL、标题、图片引用、当前价格、币种、类目路径、商品属性、采集时间、时效判断和证据引用。
- 新快照追加保存，历史快照不得被改写成当前事实。
- 销售样本按 cross_border_cn、other_cross_border、证据完整的 unknown、local_ru 背景价依次优先。
- 中国跨境卖家是优先参考，不是唯一参考。sellerType=unknown 只表示身份未确认；只要商品可比、当前价格清晰、页面可追溯且证据有效，就可以参与主要价格带。
- local_ru 可作为市场背景，但不能单独决定中国跨境采购上限或正式利润通过。
- 销售样本必须记录商品可比性、身份确认程度、采用的卖家类型、各类样本数量、价格判断可信度以及是否包含俄罗斯本土背景价。
- 市场和评价研究使用有限、具有代表性的样本，不把某个临时样本数量写成永久规则。
- Seerfar 的数值阈值用于辅助判断，不是自动淘汰的唯一硬门槛。利润优先；季节性是重要优先因素；竞争激烈的方向在供货优势和利润证据充分时仍可进入后续阶段。
- 已确认属于第三方品牌或存在明确品牌/IP 风险的商品，不进入正常无品牌或自有品牌选品流程。
- 销售端失败必须区分 data_unavailable、system_error 和 permission_required。失败不得改写为零销量、零需求或商品淘汰。

### 4.3 供货证据和来源优先级

- 选品阶段寻找和核实货源时，默认优先查询 1688；其他获准平台可以作为补充，用户也可以人工指定供应商。
- 这一优先级只约束选品阶段的找货顺序，不表示实际采购永远优先 1688，也不得限制独立采购流程按具体价格、库存、方案和风险重新判断。
- SupplierOption 至少保存来源平台、精确商品 URL、offerId、供应商销量或稳定性证据、可信标识、供应 SKU 列表、采集时间和证据引用。
- 短链只能作为原始来源引用；进入确认前必须解析并保存精确商品身份。
- 每个 SupplierSKU 至少分开保存供应 SKU ID、变体键、属性、单件商品价、单件国内运费、其他采购费用、实际采购成本、重量、尺寸、材质、电源或电池事实、图片引用及各字段来源。
- 页面没有的字段必须保持 unknown 或 null，禁止按最低价、标题、图片、总价或其他 SKU 倒推。
- 一件起订是供货方案的硬门禁。无法满足一件起订的方案不得作为当前供货方案通过。
- 搜索供应商必须有明确数量或阶段边界，不得为了寻找更低价格无限扩展搜索。
- 供应推荐可以综合实际采购成本、供应商销量和稳定性、可信标识、规格与包装一致性以及事实完整度，但系统只能生成推荐和依据，不得自动确认供应方案。
- 同款判断必须区分精确同款、近似款、错误匹配和无法判断。近似款只能作为价格或市场参考，不能冒充精确供货方案。
- 其他供应变体可以保存为备选证据，但不得与主人已确认的主 SKU 合并。以后启用其他变体时，必须建立独立候选和独立生命周期，不能继承原 SKU 的利润或上架结论。
- 采购上限必须根据目标售价、当前费用、物流和完整成本按 SKU 动态计算，不得使用永久固定采购上限。

### 4.4 主人供货确认和包装字段

- 插件或连接器采集成功只表示“已采集，等待主人选择”。默认 selectedSkuIds 为空、ownerSupplyConfirmed=false。
- 不得按最低价自动选择供应 SKU，不得自动确认供应，不得由采集成功直接进入 B。
- 主人必须在同一张 A 确认卡完成一种最终动作：
  - 淘汰商品方向；或
  - 确认商品方向、精确供应链接、具体供应 SKU、商品价、国内运费、其他采购费用、实际采购成本、重量和尺寸。
- 系统内部可以保存多个决定对象，但 UI 不得要求主人连续完成两次内容重复的确认。
- 只有精确供应链接、具体供应 SKU、商品价、国内运费、实际采购成本、打包重量、尺寸和主人供货确认全部保存后，才可以进入 B。
- B 不得重新打开供应商页面。
- 当供应商与 Ozon 的包装重量和尺寸明确属于同一个供应 SKU、来源当前有效且可追溯、字段含义一致，并且差异不影响计费重和利润时，系统可以自动确认对应的单个包装字段。
- 包装字段自动确认必须保存两侧来源和比较依据，不得因此自动确认完整供货方案；完整供货方案仍由主人确认。
- A 阶段供货确认只确认供货方案，不表示采购授权、付款授权或下单完成。

## 5. B 阶段：完整成本和正式利润

### 5.1 成本口径

- 第一次正式利润计算就必须纳入当前平台、店铺、类目、销售模式和线路下全部应计成本。
- 版本化成本策略必须明确哪些成本项目适用、采用什么数值、币种、证据和版本。
- 任一必要成本缺失、失效或没有有效数值时，不能形成正式利润结果，更不能判定利润通过或触发 B → C1。
- 条件不足时可以显示清楚标注的条件测算或证据缺口，但不得把它称为正式利润、估算利润通过、采购批准或上架批准。
- “采购到手总价”指单件商品价、国内运费和其他采购费用合计后的 actualPurchaseCost，B 只能扣除一次。
- 底层必须分别保存商品价、国内运费、其他采购费用和合计。只有合计得到确认而分项缺失时，分项保持 unknown，不得猜分。
- 各成本分项合计必须与 actualPurchaseCost 一致；已包含在卖家结算收入中的费用不得再次扣除。
- 商品件数、供应包装数、重量尺寸和成本必须统一到同一单件口径。
- 促销空间只用于从目标成交价反推建议标价，不是在折后成交价上再次扣费。
- 默认自然流量广告成本为零；只有存在真实广告计划或已发生广告支出时，才加入对应广告情景。
- 损耗预留、包装、标签、售后、支付或结算等适用成本及其数值必须来自已批准的版本化配置，不得把旧商品或旧类目的数值写成通用事实。

### 5.2 价格、平台和门槛

- 竞品折前标价、买家当前成交价、我方目标成交价和我方预计卖家结算收入必须分开记录，不得互相替代。
- Ozon 与 WB 的价格、佣金、汇率、物流和促销口径不得混用。
- 易变事实必须按当前平台、店铺、类目、销售模式、线路和 SKU 使用适用证据。
- Ozon 中国卖家后台价格字段按 CNY 写入，俄罗斯买家目标成交价按 RUB 展示。生产授权必须分别锁定后台写入价、买家目标价、币种和汇率证据；币种不匹配时禁止写入。
- 项目统一默认利润门槛为：单件利润不少于 20 元人民币，或利润率不少于 15%，满足任一项即可。
- 只有经过批准、持久保存并明确适用平台或店铺的配置可以覆盖统一默认门槛。
- 每次利润计算必须保存实际采用的门槛值、配置 ID、配置版本和适用范围，不能只记录最终通过或不通过。
- 利润率按单件利润除以 B 阶段建议成交价人民币计算。
- 未经批准的临时数值、旧类目数值或聊天示例不得覆盖默认门槛。

### 5.3 证据完整性

- B 只读取 A 冻结的销售快照、主人确认的供应 SKU、完整成本配置，以及适用键一致且有效的平台费用、物流和汇率证据。
- B 不重新打开 Ozon、WB、1688 或其他供应平台，不重新寻找供应商，不重新询问已经确认的价格、重量和尺寸，也不调用 AI 决定利润。
- 当前精确佣金无法取得时，用户可以明确允许该 SKU 使用估算佣金进行条件测算；该测算不得形成正式利润通过或推进 C1。
- 在形成正式利润结论前，仍必须取得当前店铺、类目和销售模式的有效佣金，并重新完成正式 B 计算。
- 佣金、官方汇率、物流资费、Schema 和带电运输规则可以按平台、店铺、类目、销售模式、线路、规则版本和有效时间形成可复用证据包。
- 只有适用键完全一致且证据仍有效时，证据包才可以跨 SKU 复用。
- 商品自身重量尺寸、电池类型、数量、容量、包装方式及其他危险属性仍须逐 SKU 确认。
- 平台或供应商已经明确发生变化时，受影响的旧数据立即失效，不得继续参与正式利润或生产授权。
- 各类数据多久必须主动复查尚未决定，不得在代码或长期规则中擅自写死统一时限。
- Seerfar 的聚合重量、体积或前台数据不能替代具体 SKU 的实际包装、物流、店铺佣金或结算事实。

### 5.4 ProfitModel

- ProfitModel 必须保存目标平台、店铺、建议成交价、建议标价、币种换算、卖家预计结算收入、全部应计成本、单件利润、利润率、门槛版本、结果、输入证据和完整计算公式。
- 不同平台、店铺、类目、销售模式、线路和币种不得混算。
- ProfitModel 只追加版本，不覆盖历史。
- 相同输入指纹、revision 和配置版本的重复调用必须幂等，不得生成第二个利润版本、第二次扣费或第二个 C1 交接。
- B 正式通过后，由服务端在同一原子提交中创建且只创建一次 C1 交接。
- B 未通过或成本、证据不足时，只保存对应业务结论和缺口，不得让 C1 提前出现。

## 6. C1：事实、类目、关键词与 SEO 草稿

- C1 只读取 A 冻结销售证据、主人确认的供应 SKU、B 正式利润模型、平台 Schema 和正式规则证据。
- C1 不得重访销售或供应平台，不重新计算利润，不替换 SKU，也不得改写 A/B 结果。
- exactSkuVerification、商品属性、平台类目、Schema 快照、电池判断、类目限制和合规字段必须绑定来源引用；无法确认时保持 unknown。
- 不得仅根据图片猜测材质、品牌、电池、尺寸、认证或平台属性。
- 竞品类目只能作为类目候选证据。只有目标 SKU 可比，且当前平台 Schema、店铺能力和类目规则确认适用时才能采用。
- SEO 标题、描述、五点和搜索词始终是 draft_only。每个采用词和商品主张必须保留关键词来源、事实绑定和适用位置。
- 模型不得创造不存在的功能、材质、品牌、尺寸、认证或使用效果，也不得覆盖 C1 事实。
- Seerfar 不是唯一允许的关键词证据来源，但 C1 必须读取版本化、可追溯且符合当前策略的关键词证据包。
- 关键词查询失败必须区分登录、权限、额度、网络超时、选择器变化、输入未提交、结果过期和真实零结果。
- 只有外部系统明确完成查询并返回可验证的零结果时，才能标记 true_empty。
- 外部关键词来源失败后不得在同一作业中临时换路径。只有预先批准、版本化、可测试且由新作业明确选择的多来源策略，才能使用其他正式证据。
- 不得伪造搜索量、转化、竞品共识、服务健康、额度或 Seerfar 来源。
- 关键词证据应区分标题核心词、属性和主题标签词、描述长尾词；具体评分阈值和权重进入配置与测试，不写成永久商品事实。
- 低语义匹配词无论搜索量多高，都不得进入标题或主题标签。
- 是否由本项目承担 E 完成后的搜索词复盘尚未决定。在决定前，C1 不得自动创建 E 后优化作业，也不得把调价、库存、广告或 SEO 维护视为本项目既定职责。

## 7. C2：素材与最终商品确认卡

- C2 使用三个互斥素材域：
  - assets.collected：只作分析参考，禁止进入 D；
  - assets.aiDrafts：只保存 AI 或工具草稿，不得自动升级；
  - assets.finalUploads：只保存主人确认的最终上传素材，D 只能读取这一部分。
- 最终素材确认必须锁定素材集合版本、来源授权、文件身份或稳定平台可接受地址、顺序、首图和完整性。
- 素材传输失败只改变 C2 技术状态，不得改变 C1 事实、SEO 草稿或 B 利润结论。
- C2 ready 或主人确认 finalUploads 只表示可以生成最终商品确认卡，不等于 ProductionAuthorization，也不得自动触发 D。
- 最终商品确认卡应在一个视图中整合商品名称与 SKU、主人确认供货方案、目标平台和店铺、B 建议售价与利润、C1 类目/属性/电池/合规、SEO 草稿、仅 finalUploads 素材、风险和全部 unknown 字段。
- 主人必须能够基于这一张卡作出生产前商业判断。
- 正式决定、生产授权、生产记录和 E 回读实际引用的素材必须长期保留并保持可追溯。
- 临时参考图和草稿可以按版本化、可调整的保留期限清理，具体期限不写入本文件。
- 清理前必须检查正式引用；仍被生产记录、生产授权或 E 回读引用的文件禁止删除。
- 素材清理不得破坏店铺隔离，不得跨候选、跨 SKU 或跨店删除。

## 8. 外部采集、浏览器、技术失败与 AI

### 8.1 外部采集授权

- 默认只读。任何真实外部采集作业都必须引用可持久保存、可核对且仍有效的授权记录；软件作业本身不能冒充授权。
- 公开资料采集可以按 SKU、明确批次或阶段一次授权。
- 公开采集授权必须锁定网站、允许动作、允许数量、适用 SKU 或批次、阶段、revision 和排除项。
- 在授权范围内，软件可以自行执行只读采集，不需要逐页面重复询问。
- 公开资料授权不得扩大为登录态资料读取、付费操作或任何写入授权。
- 需要现有登录态的资料必须使用独立、可保存的登录态读取授权。
- 登录态读取授权必须锁定账号平台或身份引用、SKU 或批次、动作、数量和阶段；范围内只读，禁止写入。
- 登录态读取授权不得保存 Cookie、Token、密码或完整浏览器会话秘密，也不得被解释为店铺写入授权。
- 每个外部作业只能执行授权范围内的一次受控动作。授权用尽、失效、revision 不符或身份不符时，必须产生零外部请求并明确停止。
- permission_required 必须区分缺少主人采集授权与外部平台账号权限不足。

### 8.2 浏览器和 Worker

- 公开只读信息优先使用官方 API、正式连接器或内置浏览器；只有依赖现有登录态的读取才使用用户 Chrome。
- 不同平台、店铺、登录、凭证、缓存和控制会话必须隔离。
- 同一 Chrome 用户资料同一时刻只能由一个任务控制。每轮重新读取当前标签，不复用旧 tab ID，不在多个任务间争抢同一页面。
- 插件或浏览器 Worker 只能领取由当前生命周期显式动作创建的作业。禁止启动时扫描候选池、补跑历史、无任务打开页面或自行决定采集对象。
- 领取必须原子锁定 job、candidate、SKU、revision、目标分支、mode、source、worker、版本、能力和一次性执行凭据。
- 无待处理作业时必须零页面动作和零外部访问。
- 短链或入口地址只允许由明确作业打开。最终规范地址、URL 商品身份、页面商品身份和回传身份必须一致。
- 落到登录、验证、移动页、中间页、首页、不同商品或其他非白名单页面时，只保存脱敏失败分类、导航阶段和时间，然后停止。
- 完整跳转 URL、查询 Token、Cookie、登录令牌和页面秘密不得进入回传、共享数据或 UI。
- 供应采集成功后只保存页面真实 SKU、直接价格、库存、图片和字段来源，并停在等待主人选择；插件不得自动选择供应 SKU、确认供货方案、运行 B/C1 或派发 Codex。

### 8.3 失败、重试与 true_empty

- 每条技术写入路径只允许执行一次。失败后必须说明失败层、请求是否发出、是否产生写入和最小恢复动作。
- 只有能够证明请求未发出或平台明确失败的普通技术路径，才允许主人通过新的明确动作创建一轮新的受控作业。
- unknown_outcome 不得通过再次点击确认直接重放；必须先对账。
- 系统不得自动换模型、自动换路径或静默降级。
- 技术失败、反爬、403、429、空页面、限流、登录失效、连接超时和解析失败属于证据缺口，不是商品淘汰、零销量、零关键词或工作完成。
- true_empty、零销量、零关键词、无库存或无结果，只能来自外部系统明确完成查询并返回可验证的零结果。
- “运行中”必须有真实 jobId、开始时间、当前步骤和最近实质进展。没有新证据、状态变化或明确阻塞，不得续期、重领或重复汇报。
- 禁止只做健康巡检却反复消耗资源。一次有证据的阻塞后转为明确等待状态，不得定时盲重试。
- E 阶段按配置进行的有限、纯只读状态查询是唯一窄例外；它不构成 D 写入重试，也绝不能重新发送写请求。

### 8.4 正式 AI 网关

- 正常 AI 能力必须通过独立、可替换的正式 AI 网关提供。评审台、浏览器、员工客户端和领域模块不得读取模型 Key。
- 每个 AI 任务必须锁定 project、candidate、SKU、revision、目标分支、业务阶段、任务类型、预先选择的模型角色、输出 Schema、输入证据引用、请求指纹和 attempt=1。
- 具体供应商和模型名必须配置化；失败后不得自动换模型、自动重试或使用聊天生成结果兜底。
- 每次调用必须形成持久化 InferenceReceipt，保存供应商请求 ID、模型和协议版本、输入证据、输出 Schema、校验结果、时间、精确失败层和供应商真实返回的用量。供应商未返回用量时记 unknown。
- 允许发送的输入只包括公开商品文字、主人已确认的商品事实和主人授权分析的商品图片。
- 永久禁止发送 Token、Cookie、密码、验证码、店铺凭证、客户或订单个人信息、未脱敏后台截图、完整生产授权正文及无关 SKU 数据。
- AI 只承担公开文字整理、属性结构化辅助、可比性辅助、获准图片的可见事实识别、翻译、SEO 草稿和复杂证据解释。
- 采集、供应 SKU 选择、利润计算、费用/物流/汇率读取、最终业务通过、生产授权、店铺写入和 E 回读必须由确定性软件或主人完成。

## 9. 生产授权、平台写入与部署

### 9.1 授权关系

- 主人商业确认、A 阶段供货确认、真实外部采集授权、ProductionAuthorization、unknown_outcome 对账或恢复授权以及代码部署授权，是相互独立的授权面。
- ProductionAuthorization 是主人针对准确平台、店铺、SKU、字段、价格、库存、素材和范围作出的生产写入授权。
- execution intent 是软件在写入前持久化的一次性执行意图，不是第二次人类授权。
- 凭证和 Worker 能力属于技术运行条件，不是商业确认或生产授权。
- 任一授权不得自动扩大到其他平台、店铺、SKU、字段、素材、阶段、执行轮次或付费动作。

### 9.2 最终生产授权

- 主人对最终商品确认卡选择“通过进入生产授权”后，才生成不可变的 ProductionAuthorization。
- 授权至少锁定平台、店铺身份、skuPackageId、supplierSkuId、merchant SKU、标题版本、属性版本、建议售价、最终写入价及币种、库存、finalUploads 版本和顺序、发布或审核范围、排除项、dataRevision 和授权身份。
- ProductionAuthorization 不得原地修改店铺、SKU、价格、素材、范围或排除项。变化必须生成新 authorizationId 和新 revision，旧版本保留。
- ProductionPlan 只能由 ProductionAuthorization 生成。D 不得重新读取 A/B/C 原始数据、重新寻找商品信息或静默修正授权字段。
- D 执行前必须校验授权 ID、授权指纹、ProductionPlan 绑定、candidate/SKU revision、目标平台、店铺身份和当前执行意图；任一漂移都必须在平台请求前拒绝。
- PlatformWritePreflight 只读 ProductionPlan，检查目标平台、店铺身份、权限、连接、可写字段、素材能力、库存能力和风险。
- 前检失败只保存技术状态，不创建商品、不上传图片、不改库存、不生成 ProductionRecord，也不改变商品业务结论。

### 9.3 平台与店铺隔离

- 平台、稳定店铺身份、SoftwareJob、Worker 能力、credential alias、生产计划和独立回读对象必须一致。
- 显示名称不能单独充当店铺身份。
- supplier SKU、merchant SKU 和 platform productId 必须分别保存，不得互相替代。
- 跨店凭证、缓存、限流、回执、素材地址、幂等键和轮询租约不得复用。
- 即使不同店铺使用相同 merchant SKU，也不得串用凭证、仓库、写入回执、素材或 E 回读结果。
- 同一供货证据进入不同平台或店铺时，必须从 ProfitModel 开始形成独立目标分支。

### 9.4 正式写入路径

- 默认只读。商品卡、媒体、价格、库存、发布、审核、归档或其他生产写入必须得到准确对象和范围下的 ProductionAuthorization。
- 写入前必须确认平台、店铺、商品、SKU/货号、字段、价格、库存、素材集合、发布范围和排除项。
- Ozon 新品生产正常路径使用正式 Seller API 完成商品字段、CNY 价格、包装、提交和独立回读。
- Ozon 浏览器仅用于正式 API 当前不能承载、且已经单独授权的本地最终素材选择或明确异常；不得成为逐字段填表的正常路径。
- WB 正常生产写入只能使用正式官方 API 适配器。
- WB 受控浏览器只用于辅助核对和排障，不承担、补做或证明日常正式写入。
- 若最终素材已有可验证、平台可接受的稳定地址，应由正式 API 处理。
- D 必须先持久化单次执行意图，再由店铺隔离的正式适配器按授权执行一次。
- 任一步外部结果不明都进入 unknown_outcome。没有真实平台商品身份、完整回执和授权一致性证明时，不得生成成功 ProductionRecord，也不得重复写入。
- Token、Cookie、密码、验证码和带密钥 URL 不得进入聊天、项目文件、日志、截图、代码注释或提交。

### 9.5 部署授权

- 开发完成、测试通过、部署包生成、子任务验收或聊天中的“可以部署”表达，都不自动构成部署授权。
- 聊天只能登记部署意图。
- 系统必须根据部署意图生成可持久保存、可核对的部署确认卡。
- 用户再次确认当前部署确认卡后，才允许生成和执行本轮部署授权。
- 部署目标、范围、文件、运行服务或版本发生变化时，原确认不得自动扩大，必须重新生成并确认准确卡片。
- 部署前后必须保护共享数据和用户刚保存的内容，保存可恢复点，并回读运行版本、健康、页面、插件或 Worker 能力、自动化状态和活跃作业。
- 部署成功不能替代平台业务回读，也不能证明任何当前 SKU 已完成。

## 10. E 阶段：独立回读和结果分类

### 10.1 三类结果

- 写入后必须按证据严格区分：
  - 本次创建成功；
  - 平台外部已有商品；
  - 结果未知。
- 只有能够证明平台商品由本轮授权和执行创建，并且存在真实 ProductionRecord 和独立平台回读时，才进入系统创建路径。
- 平台存在商品但无法证明由本轮创建时，只能进入 ExternalListingRecord 路径，不得冒充 ProductionRecord。
- 写入请求可能已发出但无法证明终态时，保持 unknown_outcome，并禁止重复写入。
- 网络中断、时间接近、货号相同、页面突然出现商品或聊天记录，都不能单独证明本轮创建成功。

### 10.2 系统创建路径

- 系统创建路径为：

  ProductionRecord → 独立平台回读 → listed_verified

- ProductionRecord 必须绑定本轮真实执行回执。
- E 必须独立核对平台商品身份、价格、库存、图片、审核、校验、错误和销售状态。
- 只有逐项证据满足当前验证合同后，才能标记 listed_verified。

### 10.3 外部发现路径

- 外部发现路径为：

  ExternalListingRecord → 独立E验证 → externally_verified

- 不得仅凭创建时间接近、货号相同、聊天记录或“看起来像本轮商品”生成 ProductionRecord。
- 外部发现记录必须保存平台商品 ID、发现来源、是否能证明由本轮创建、merchant SKU、supplier SKU、价格、币种、库存、图片、审核状态、错误、回读时间和证据引用。

### 10.4 有限只读查询

- 平台已经明确接受请求并返回稳定任务或商品身份、但审核仍在处理时，可以显示 waiting_platform。
- E 可以按版本化配置，对准确平台对象进行有限次数、有限时间的纯只读状态查询。
- 查询范围必须绑定平台、店铺、稳定任务或商品身份、原 revision 和原授权对象。
- 每次查询必须保存观察结果；达到次数或时间上限后立即停止。
- 平台明确仍在处理时继续显示“等待平台”；无法证明写入终态时保持“结果未知”或 unknown_outcome。
- 有限查询不构成 D 写入重试，绝不得重新发送写请求、补写字段或自动修正平台差异。
- E 发现价格、库存、素材或状态不一致时，只记录差异；修正必须重新取得准确授权并创建新的 D 动作。
- 请求成功、导入任务 ID、脚本退出成功、上传数量、本地页面变化或监控发现对象，都不能替代 E 独立回读。
- E 只验证平台事实，不反向改写 ProductionAuthorization 或历史 D 回执。
- E 完成以后是否继续承担调价、库存、广告或 SEO，尚未决定。在决定前不得自动扩展本项目职责或自动创建相关作业。

## 11. 每日计划、评审台与配置边界

- 每日最低目标必须保存在可调整、可追溯的版本化配置中，不得作为永久常量写死在代码或长期规则里。
- 当前每日目标的实际数值只以当次启用的计划和配置为准；以后调整目标不需要修改 AGENTS.md 或重写代码。
- 自动选品只有在用户启用一份持久化每日计划后才允许运行。
- 每日计划必须限定业务日期、目标店铺、来源、数量、付费额度、revision 和适用授权。
- 每日计划只允许在批准范围内自动运行；没有启用计划时，不得自动采集、调用付费服务或产生业务推进。
- 每日计划不得代替登录态资料的独立授权，也不得扩大平台写入或采购权限。
- 页面应分别统计正式利润通过、条件测算、C1/C2 完成、具备生产授权条件以及真正平台已验证的数量，不得混成一个“已完成”。
- 所有新品库存的当前默认值为 100，但 C2 只能预填并展示，不得产生库存写入。
- D 必须在准确商品的最终生产确认卡中锁定并确认实际库存值；默认值本身不构成写入授权。
- 评审台必须展示人类真正需要的结论、完整算式、证据、负责人、四条状态线、卡点和下一步，不要求人类通过阅读技术日志理解系统。
- 页面至少要区分软件处理中、AI 分析中、等待主人确认、外部依赖不可用、已知技术失败并安全停止、等待平台、unknown_outcome 待对账、ExceptionCase、Codex 真实维护中、业务淘汰或通过以及平台已回读验证。
- “Codex 介入中”必须有真实维护轮次；“运行中”必须有 jobId、开始时间、当前步骤和最近实质进展。
- 页面应展示销售快照版本与时效、卖家类型与可比性、供应链接与具体 SKU、成本分项、重量尺寸、B 利润版本和完整算式、目标平台和店铺、C1/C2 交接、生产授权及 E 回读证据。
- AGENTS.md 只规定必须展示的业务含义、状态区别、证据和授权门禁。具体页面分组、按钮、排序和交互由产品设计与配置决定。
- 修改 UI、状态机或业务规则时必须保护共享候选数据，保持 revision 校验和原子持久化，不得通过部署覆盖人类刚保存的数据。
- 尚未验证的想法只能标记为待验证或实验规则，不能成为自动通过、自动淘汰或生产写入门禁。

## 12. 文档、施工、测试与交接纪律

- 当前主流程由长期规则、中央或评审台持久化状态和正式平台回读共同约束，不创建只存在于聊天中的第二套流程。
- 文档分层固定为：
  - AGENTS.md：所有任务必须遵守的长期业务与工程不变量；
  - PROJECT_STATE.md：由当前代码、测试或正式运行证据支持的架构、能力、业务口径、已知问题和验收方法；
  - HANDOFF.md：当前施工现场、剩余工作、阻塞和下一步。
- 项目根目录`CURRENT.md`是上述权威载体和当前老板决定索引的唯一导航入口，只负责指路和新旧边界，不保存业务状态，也不得成为新的事实源。
- 常规任务默认只读取`CURRENT.md`列出的当前载体以及与任务直接相关的源码、Schema、配置和测试。`docs/archive/`默认排除，不得通过全目录搜索把历史原文重新混入普通任务上下文。
- 只有主人明确要求历史追溯、冲突核对、来源恢复或删除审计，或者当前索引给出精确档案引用时，才允许读取`docs/archive/`中的指定文件。历史档案中的旧规则、旧OPEN、旧数值、旧授权和旧实现结论永远不能覆盖当前规则、当前代码证据或当前业务状态。
- 历史文件可以无损移动到档案区，但不得因为已经归档就自动删除。删除任何历史文档、代码、脚本、接口或数据前，必须核清当前消费者、运行副本、替代能力、恢复方式和影响，并取得主人针对精确路径的确认。
- 候选数量、当前 revision、测试计数、哈希、部署状态、端口、当前模型、当前插件版本和单商品事实不得写成长期不变量。
- selection-review-app/README.md 只说明实现和运行方法，不能覆盖业务长期规则或实时业务事实。
- 现有专业 Skill 可以作为定价、平台操作或验证工具，但不得覆盖本文件的职责边界、当前持久化状态或生产确认门禁。
- 不为每次讨论另建说明文档。可以进入现有规则、配置、代码、测试或 UI 的内容应进入唯一正确载体。
- 无法由当前代码、测试、正式运行证据或用户明确决定支持的内容，必须标记 UNVERIFIED，不得写成项目已完成事实。
- 本文件中的工程路径是强制目标合同和验收边界，不代表当前 C1、D、E 或其他阶段已经部署或完成软件化。
- 正常 SKU 的规范路径是：中央状态产生明确软件作业，作业锁定 candidate、SKU、revision、阶段、输入版本、目标平台、店铺和允许范围；正式连接器、Worker 或 AI 网关执行一次受控动作；结构化结果经 Schema、身份、revision 和证据校验后原子保存，再由软件决定是否推进。
- 无作业时必须零页面动作、零外部访问、零模型调用和零店铺写入。
- 对话、子任务和交接消息只用于施工协调、异常诊断和事实线索。接收方必须重新读取当前持久化状态、revision、授权和软件作业终态。
- 正式 AI、连接器和 Worker 只提供事实、能力状态和精确失败分类，不得选择供应 SKU、计算利润、决定业务通过、创建生产授权或改变生命周期阶段。
- 跨阶段数据必须继承同一供应 SKU、目标平台、店铺和冻结版本。B 只读 A；C1 只读 A/B；C2 不改写 C1；D 只读 ProductionAuthorization；E 独立读取平台。
- 测试和构建必须使用项目已验证的运行时与依赖，不得通过联网升级、删除依赖、降低断言、跳过测试、修改批准策略、关闭检查或 suppress 来制造通过。
- 受限环境无法完成的验证必须准确标记为环境限制，并在允许的真实环境运行同一验证；不得把环境失败冒充代码通过，也不得反复空跑。
- 开发完成、领域测试通过、队列完成、页面正常或子任务回复，均不等于业务完成。
- 长对话交接只保留最高原则、权威状态入口、唯一负责人、已验证完成项、真实未完成项、阻塞、重要决定、风险、授权边界及相关代码、配置、Schema、测试和状态位置。
- 不得继续复制旧 tab ID、旧草稿 ID、旧价格、旧哈希、过期版本或已经结束的技术分支。
- 多对话用于分离开发、审查、连接器、运行时、发布和异常维修的文件所有权，不得建立多个正常 SKU 生产消费者。

## 13. 当前仍悬而未决的事项

本节只保存已经明确决定“暂不决定”的事项。它们不得被解释为执行授权、默认配置或已经完成的设计。

### 13.1 三店名称和稳定身份写在哪里

- 状态：悬而未决。
- 已确定边界：
  - 每个平台和店铺必须具有稳定身份；
  - 利润、授权、作业、写入、凭证和 E 回读必须按平台和店铺隔离；
  - 显示名称不能单独代替稳定店铺身份。
- 尚未决定：
  - 三店名称、平台店铺 ID、sellerId、credential alias 等身份信息由哪个正式载体保存；
  - UI、配置和中央数据库之间如何引用。
- 决定前不得：
  - 擅自创造店铺名称、ID 或映射；
  - 使用默认店铺或邻近店铺兜底；
  - 把店铺显示名当作唯一身份。
- 下一步需要核验：
  - 当前官方平台账号和店铺身份；
  - 当前配置及共享数据中的既有映射；
  - 不包含秘密的稳定身份字段和引用方式。

### 13.2 各类证据多久主动复查

- 状态：悬而未决。
- 已确定边界：
  - 平台、供应商或规则已经明确发生变化时，相关旧数据立即失效；
  - 已失效数据不得继续参与正式利润、生产授权或平台写入。
- 尚未决定：
  - 销售价格、供应价格、库存、佣金、汇率、物流、Schema、合规和素材分别多久必须主动复查；
  - 不同平台、店铺、类目和阶段是否使用不同周期。
- 决定前不得：
  - 擅自写死统一天数或统一 TTL；
  - 把没有明确变化证据的数据自动判定为永久有效。
- 下一步需要核验：
  - 各类数据的真实变化速度；
  - 平台规则和正式接口提供的有效期；
  - 不同数据过期对利润和写入风险的实际影响。

### 13.3 多人使用时的查看、确认和执行权限

- 状态：悬而未决。
- 已确定边界：
  - 中央服务必须支持稳定用户身份、权限检查和可追溯操作记录；
  - 当前主人确认、供货确认、生产授权和部署授权不能自动扩展给其他人员；
  - 店铺数据和凭证必须继续隔离。
- 尚未决定：
  - 哪些人可以查看哪些店铺；
  - 哪些人可以确认供货、利润、生产、异常恢复或部署；
  - 是否需要按店铺、阶段或字段进一步限制。
- 决定前不得：
  - 创建默认角色权限矩阵；
  - 因为员工可以查看某家店，就推定其可以确认或写入；
  - 因为具备技术部署权限，就推定其具备业务授权。
- 下一步需要核验：
  - 实际参与人员和职责；
  - 店铺分工；
  - 必须由主人保留的决定；
  - 多人协作中的最小权限需求。

### 13.4 E 完成后的项目职责

- 状态：悬而未决。
- 已确定边界：
  - E 负责独立验证本次上架或外部已有商品的真实平台状态；
  - E 完成不会自动授权新的价格、库存、广告、SEO 或素材写入。
- 尚未决定：
  - E 后的调价、库存、广告、SEO、关键词复盘和商品维护继续由本项目承担，还是交给独立店铺运营流程；
  - 是否需要正式交接对象或新的生命周期阶段。
- 决定前不得：
  - 自动创建 E 后调价、库存、广告或 SEO 作业；
  - 把这些工作写成已经确定的 F 阶段；
  - 使用原 ProductionAuthorization 继续执行后续运营写入。
- 下一步需要核验：
  - 现有店铺运营项目的职责；
  - E 后各类操作需要的独立证据和授权；
  - 是否需要正式交接记录。
